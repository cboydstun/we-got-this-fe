import React from 'react';

import { Paper } from '@material-ui/core';

import { makeStyles } from '@material-ui/core/styles';

const JobPhoto = ({ photo }) => {
    return <div>Photo and note</div>;
};

export default JobPhoto;
