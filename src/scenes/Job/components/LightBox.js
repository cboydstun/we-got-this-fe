import React from 'react';
import { Dialog } from '@material-ui/core';

const LightBox = ({ src }) => {
    return (
        <>
            <p>LightBox</p>
        </>
    );
};

export default LightBox;
